from pyspark.sql import SparkSession
from pyspark.sql.types import StructType, StructField, IntegerType, FloatType, StringType, TimestampType
import os

# ✅ Τοπικό μονοπάτι προς το αρχείο CSV (π.χ. στον ίδιο φάκελο με το script)
csv_file_path = "yellow_tripdata_2024.csv"

# 📍 Ο φάκελος εξόδου για τα αρχεία Parquet
output_path = "output_parquet/yellow_tripdata_2024"

# 🚀 Εκκίνηση SparkSession
spark = SparkSession.builder \
    .appName("Convert CSV to Parquet - Local PyCharm") \
    .master("local[*]") \
    .getOrCreate()

spark.sparkContext.setLogLevel("ERROR")

# 📐 Ορισμός schema βάσει του dataset yellow_tripdata_2024
trip_schema = StructType([
    StructField("VendorID", IntegerType()),
    StructField("tpep_pickup_datetime", TimestampType()),
    StructField("tpep_dropoff_datetime", TimestampType()),
    StructField("passenger_count", IntegerType()),
    StructField("trip_distance", FloatType()),
    StructField("RatecodeID", IntegerType()),
    StructField("store_and_fwd_flag", StringType()),
    StructField("PULocationID", IntegerType()),
    StructField("DOLocationID", IntegerType()),
    StructField("payment_type", IntegerType()),
    StructField("fare_amount", FloatType()),
    StructField("extra", FloatType()),
    StructField("mta_tax", FloatType()),
    StructField("tip_amount", FloatType()),
    StructField("tolls_amount", FloatType()),
    StructField("improvement_surcharge", FloatType()),
    StructField("total_amount", FloatType()),
    StructField("congestion_surcharge", FloatType()),
    StructField("airport_fee", FloatType())
])

# 📥 Διαβάζουμε το CSV
df = spark.read.format("csv") \
    .option("header", "false") \
    .schema(trip_schema) \
    .load(csv_file_path)

# 🔍 Προβολή 5 πρώτων εγγραφών
df.show(5)
df.printSchema()

# 💾 Αποθήκευση ως Parquet
df.write.mode("overwrite").parquet(output_path)

print(f"✅ Τα δεδομένα αποθηκεύτηκαν σε Parquet στον φάκελο: {output_path}")
